-Colors concept is used to give eye catching appearence  to the  html documennt.
-Color Combination
-Color property
-Border
-Background
-Text/Content

----HTML LIST-----
   -coolection of Items
   -Types of list:-
    1.ordered
    2.unordered
    3.Discripted list
--Ordered list
  -used to Display items in sequence
  -Tags:-
    -OL--ordered nList
    --Parent Element -- paired Tag
    --LI-List Item --Child Elemnt of OL
--Descriptive LList:-
 -used to DIsplay list with some information.
 -Tags:-
    DL--Derscriptive List --parent Element
    DT--Descriptive Name--Child Element OF DL
    DD--Description Detail ----Used to give information
HTML Caomputer Code:-
  -used to Display code
  -Used to display the sample input or sample output
  -uded to disply the content getting from the user during run time.
  --Tags:-
   1.<code> :-
        -paired Tag
        -used to print the code.
        -content displayed in otput varies from normal output.
        -Font style and font size may vary
        -Syntax
         <code>Program Logic</code>
        -for example:-
  --Variables
   -used to give some effect to the variable
   -Variable is represented with slightly tilde right side
   -Always put the variable inside the var tag.
   Syntax;-

   3.pre --pre formatting tags
     ---print content as it as.
  4.KBD--KEYBOARD Input
   ---used to represent content taken from user input
    --paired Tag
    Syntax:-
      <kbd></kbd>
  

  ---------Class------------------
      -Attribute to give effects to the elements in html document.
      -you can apply effects to different elements
   -inside single html doc.
      -class is a collection of property name and property value.
       Syntax:-
          -use style tag inside head tag.
      dot(.)classname{
        property name:property value.
        .......
        ....
        ....
      }
      class concept is used in:-
      -Internal
      -External
      -Jvascript

-- BookMark
    -used to jump from point to another point
    -In hard dicuments you can use the concept of book mark by placing some tag from wgere you want to continue text
    -In soft Documents you use jumping statement(C language)
    -In Html documrent you can achieve the concept of book mark with the help of ID concept
    -Syntax:
         -create one link by which you can reach to specific point.
         -Link can be on text or other content
         -create Link with anchor tag.


  --HTML FORM:-
   -Form is type of container in which if youneant to get the details from the user
--Details:
  . Text
  . Text box -Email,password.
  .Radio -Gender
  .Dropdownlist -Country name
  .state name
  .city name
  .Fileupload--
  .Image
  .Document
  --you can use more than one form inside HTML Document
  --Avoid Using Nesting form
  --If you are using more than one form then form must be uniquely identified.
--Tag
 <form></form>
 --Attributes
   -accept-charser: used to accept the character encoding method
   -action: when be click on submit button then the form redirect which location(For Redirecting to page)
   -name :for the reference purpose same as id(it is used to give the name of the form)
   -name in input tag-to get the data in ther form of url on other page.
   query String:-
   ? -Starting of query String
   method:-
     .get :-Not secure
       -dont pass sensitive data
       -small amount of data can be pass
       -2048 Characters use in this method
      .post :-Secure  Data
    . Autocomplete
    .novalidate
    -Target
      -_blank
      -_top
      -_self
      -_parent
      -_custom

--Jvascript:-
 javascript is a object based programming langauage
  -object oriented:-
   class
   object
   polymorphism
   Abstraction
   Inheritance
   Encapsulation
  -object based 
  -Inbuilt
  -Do not achieve object oriented concepts
   -javascript is object Based.
      -To make page dymnamic
      -To get the data during run time
      -To display data during run time
      -To make pages user friendly
1.to represent the content in the elements during run time.
2.Giving Formating to elements with javascript.
3.cssText--property --parent--cssStyleDeclaration
    -value---String
4.
    

